# คู่มือการติดตั้งระบบเว็บไซต์โรงเรียน
## School Website Deployment Guide

คู่มือนี้จะแนะนำวิธีการนำเว็บไซต์ของโรงเรียนไปติดตั้งใช้งานกับโรงเรียนอื่นๆ โดยจะรวมถึงการสำรองและติดตั้งฐานข้อมูล (โครงสร้างเท่านั้น ไม่มีข้อมูล) และโค้ดทั้งหมด

---

## 📋 สารบัญ

1. [สิ่งที่ต้องเตรียม](#สิ่งที่ต้องเตรียม)
2. [ส่วนที่ 1: สำรอง Database Schema](#ส่วนที่-1-สำรอง-database-schema)
3. [ส่วนที่ 2: เตรียมโค้ด](#ส่วนที่-2-เตรียมโค้ด)
4. [ส่วนที่ 3: ติดตั้งสำหรับโรงเรียนใหม่](#ส่วนที่-3-ติดตั้งสำหรับโรงเรียนใหม่)
5. [ส่วนที่ 4: ปรับแต่งเบื้องต้น](#ส่วนที่-4-ปรับแต่งเบื้องต้น)

---

## สิ่งที่ต้องเตรียม

### สำหรับผู้ดูแลระบบเดิม (คนที่จะ Backup)
- ✅ เข้าถึง Supabase Dashboard
- ✅ เข้าถึงโค้ดในเครื่อง (`/Users/krisadakhamma/React/kk-school-main`)
- ✅ โปรแกรม Git (ถ้ามี)

### สำหรับโรงเรียนใหม่ (คนที่จะติดตั้ง)
- ✅ บัญชี GitHub (ฟรี) - https://github.com
- ✅ บัญชี Supabase (ฟรี) - https://supabase.com
- ✅ บัญชี Vercel (ฟรี) - https://vercel.com
- ✅ โปรแกรม Git
- ✅ Node.js เวอร์ชัน 18 ขึ้นไป

---

## ส่วนที่ 1: สำรอง Database Schema

### 1.1 Export Schema จาก Supabase Dashboard

**วิธีที่ 1: ใช้ SQL Editor (แนะนำ)** 

1. เข้า Supabase Dashboard → โปรเจกต์ของคุณ
2. ไปที่เมนู **SQL Editor**
3. สร้าง Query ใหม่และรันคำสั่งนี้:

```sql
-- Export schema เท่านั้น (ไม่รวมข้อมูล)
SELECT 
    table_name,
    column_name,
    data_type,
    is_nullable,
    column_default
FROM information_schema.columns
WHERE table_schema = 'public'
ORDER BY table_name, ordinal_position;
```

4. **สำคัญ**: ทางที่ดีที่สุดคือการใช้ไฟล์ Migration ที่มีอยู่แล้วในโฟลเดอร์ `supabase/migrations/`

### 1.2 ใช้ไฟล์ Migration ที่มีอยู่

โครงการนี้มี Migration Files ครบถ้วนอยู่แล้วใน:
```
supabase/migrations/
├── 001_initial_schema.sql           # โครงสร้างพื้นฐาน (News, Gallery)
├── 002_events_and_settings.sql      # Events และ Settings
├── 003_administrators.sql           # ข้อมูลผู้บริหาร
├── 004_staff.sql                    # ข้อมูลบุคลากร
├── 005_students.sql                 # ข้อมูลนักเรียนและสภานักเรียน
├── 006_admissions.sql               # ระบบรับสมัคร
├── 007_curriculum.sql               # หลักสูตรและกิจกรรม
├── 008_contact_messages.sql         # ข้อความติดต่อ
├── 009_news_external_links.sql      # ลิงก์ภายนอกสำหรับข่าว
└── 010_increment_views.sql          # ฟังก์ชันนับจำนวนผู้เข้าชม
```

**ไฟล์เหล่านี้คือ "แบบพิมพ์เขียว" ของ Database ที่สามารถนำไปใช้ซ้ำได้**

---

## ส่วนที่ 2: เตรียมโค้ด

### 2.1 สร้าง Package สำหรับแจกจ่าย

**ขั้นตอน:**

1. **ลบไฟล์ที่ไม่จำเป็น:**
   ```bash
   cd /Users/krisadakhamma/React/kk-school-main
   
   # ลบ node_modules (จะติดตั้งใหม่ภายหลัง)
   rm -rf node_modules
   
   # ลบ dist (build output)
   rm -rf dist
   
   # ลบ .env (จะสร้างใหม่สำหรับแต่ละโรงเรียน)
   rm .env
   ```

2. **สร้างไฟล์ `.env.example`:**
   ```bash
   cat > .env.example << 'EOF'
# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
EOF
   ```

3. **ตรวจสอบ `.gitignore`:**
   ```bash
   cat .gitignore
   # ต้องมี:
   # node_modules
   # dist
   # .env
   # .DS_Store
   ```

### 2.2 สร้าง Repository บน GitHub (แนะนำ)

**วิธีที่ 1: ใช้ Git (แนะนำ)**

```bash
# 1. สร้าง Repository บน GitHub ก่อน (ชื่อ เช่น school-website-template)

# 2. เข้าโฟลเดอร์โปรเจกต์
cd /Users/krisadakhamma/React/kk-school-main

# 3. เริ่มต้น Git (ถ้ายังไม่มี)
git init

# 4. เพิ่มไฟล์ทั้งหมด
git add .

# 5. Commit
git commit -m "Initial commit - School website template"

# 6. เชื่อมต่อกับ GitHub
git remote add origin https://github.com/YOUR_USERNAME/school-website-template.git

# 7. Push ขึ้น GitHub
git branch -M main
git push -u origin main
```

**วิธีที่ 2: แชร์แบบ ZIP File (สำหรับคนที่ไม่ใช้ Git)**

```bash
cd /Users/krisadakhamma/React
zip -r school-website-template.zip kk-school-main \
  -x "*/node_modules/*" \
  -x "*/dist/*" \
  -x "*/.env" \
  -x "*/.git/*"
```

จากนั้นแชร์ไฟล์ `school-website-template.zip` ให้โรงเรียนอื่น

---

## ส่วนที่ 3: ติดตั้งสำหรับโรงเรียนใหม่

### 3.1 เตรียม Supabase Project ใหม่

1. **สร้าง Project ใหม่:**
   - เข้า https://supabase.com
   - คลิก **New Project**
   - ตั้งชื่อโปรเจกต์ (เช่น `school-xyz`)
   - เลือก Region (แนะนำ `Southeast Asia (Singapore)`)
   - ตั้งรหัสผ่าน Database (เก็บไว้ดีๆ)
   - คลิก **Create new project**

2. **เก็บ API Keys:**
   - เข้าเมนู **Settings** → **API**
   - คัดลอก:
     - `URL` (Project URL)
     - `anon/public` key

### 3.2 Import Database Schema

1. เข้า Supabase Dashboard → **SQL Editor**
2. รันไฟล์ Migration ทีละไฟล์ตามลำดับ:

```sql
-- 1. รันไฟล์ 001_initial_schema.sql
-- (คัดลอกเนื้อหาทั้งหมดจากไฟล์มาวางและกด Run)

-- 2. รันไฟล์ 002_events_and_settings.sql
-- 3. รันไฟล์ 003_administrators.sql
-- ... รันต่อไปเรื่อยๆ ตามลำดับเลขไฟล์
```

> **💡 เคล็ดลับ:** คุณสามารถรันทีละหลายไฟล์โดยวางเนื้อหาทั้งหมดในครั้งเดียว

3. **ตรวจสอบว่า Tables ถูกสร้างแล้ว:**
   - ไปที่เมนู **Table Editor**
   - ควรเห็น Tables: `news`, `gallery_albums`, `administrators`, `staff`, `students`, `admissions`, ฯลฯ

### 3.3 ติดตั้งโค้ดในเครื่อง

**ถ้าได้รับ ZIP File:**

```bash
# 1. แตก ZIP
unzip school-website-template.zip
cd school-website-template

# 2. ติดตั้ง Dependencies
npm install
```

**ถ้าได้รับ GitHub Repository:**

```bash
# 1. Clone Repository
git clone https://github.com/USERNAME/school-website-template.git
cd school-website-template

# 2. ติดตั้ง Dependencies
npm install
```

### 3.4 ตั้งค่า Environment Variables

```bash
# 1. สร้างไฟล์ .env จาก .env.example
cp .env.example .env

# 2. แก้ไข .env ใส่ค่าจาก Supabase
nano .env
# (หรือเปิดด้วย Text Editor ที่ชอบ)
```

**เนื้อหาใน `.env`:**
```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here
```

### 3.5 ทดสอบในเครื่อง

```bash
# รันเซิร์ฟเวอร์ Development
npm run dev
```

เปิดเบราว์เซอร์: http://localhost:5173

ตรวจสอบว่า:
- ✅ หน้าเว็บโหลดขึ้นมาได้
- ✅ Admin Login ใช้งานได้ (`/admin`)
- ✅ เชื่อมต่อ Database สำเร็จ

---

## ส่วนที่ 4: ปรับแต่งเบื้องต้น

### 4.1 สร้าง Admin Account แรก

1. เข้า Supabase Dashboard → **Authentication** → **Users**
2. คลิก **Add user** → **Create new user**
3. กรอก Email และ Password
4. คลิก **Create user**
5. จดอีเมลและรหัสผ่านไว้

### 4.2 ตั้งค่าข้อมูลพื้นฐานของโรงเรียน

1. เข้าหน้าเว็บ → ไปที่ `/admin`
2. Login ด้วย Admin account ที่สร้างไว้
3. ไปที่แท็บ **ตั้งค่าระบบ (Settings)**
4. กรอกข้อมูล:
   - ชื่อโรงเรียน
   - คำขวัญ
   - คำอธิบาย
   - ที่อยู่
   - เบอร์โทร
   - อีเมล
   - ปีการศึกษาปัจจุบัน
5. กด **บันทึกการตั้งค่า**

### 4.3 อัปโหลดโลโก้และรูปภาพ

1. ใน Admin Panel → แต่ละเมนู มีปุ่ม **เลือกรูป**
2. อัปโหลดรูปโลโก้โรงเรียน
3. อัปโหลดรูปภาพต่างๆ ตามต้องการ

---

## ส่วนที่ 5: Deploy ขึ้น Production

### 5.1 Deploy ด้วย Vercel (แนะนำ - ฟรี)

**ขั้นตอน:**

1. **Push โค้ดขึ้น GitHub** (ถ้ายังไม่ได้ทำ)
   ```bash
   git add .
   git commit -m "Configure for School XYZ"
   git push
   ```

2. **เข้า Vercel:**
   - ไปที่ https://vercel.com
   - Login ด้วย GitHub
   - คลิก **Add New** → **Project**
   - เลือก Repository ของโรงเรียน
   - คลิก **Import**

3. **ตั้งค่า Environment Variables:**
   - ในหน้า Project Settings
   - ไปที่ **Environment Variables**
   - เพิ่ม:
     - `VITE_SUPABASE_URL` → ใส่ URL ของ Supabase
     - `VITE_SUPABASE_ANON_KEY` → ใส่ Anon Key

4. **Deploy:**
   - คลิก **Deploy**
   - รอประมาณ 1-2 นาที
   - เสร็จแล้วจะได้ URL เช่น `https://your-school.vercel.app`

### 5.2 ตั้งค่า Domain (ถ้ามี)

1. ใน Vercel Project → **Settings** → **Domains**
2. เพิ่ม Domain ของคุณ (เช่น `www.school-xyz.ac.th`)
3. ปรับ DNS ตามคำแนะนำของ Vercel

---

## 📁 โครงสร้างโปรเจกต์

```
kk-school-main/
├── src/                      # โค้ด React
│   ├── components/           # Component ต่างๆ
│   │   ├── admin/           # Admin Panel
│   │   └── ui/              # UI Components
│   ├── pages/               # หน้าเว็บต่างๆ
│   ├── hooks/               # React Hooks
│   └── integrations/        # Supabase Integration
├── supabase/
│   └── migrations/          # ไฟล์ SQL สำหรับสร้าง Database
├── public/                  # ไฟล์สาธารณะ
├── .env                     # ตั้งค่า (ไม่แชร์)
├── .env.example             # ตัวอย่างการตั้งค่า
├── package.json             # Dependencies
└── README.md               # คำอธิบายโปรเจกต์
```

---

## 🔧 คำสั่งที่สำคัญ

| คำสั่ง | คำอธิบาย |
|--------|----------|
| `npm install` | ติดตั้ง Dependencies |
| `npm run dev` | รัน Development Server |
| `npm run build` | Build สำหรับ Production |
| `npm run preview` | ทดสอบ Build ในเครื่อง |

---

## 🆘 แก้ปัญหาเบื้องต้น

### ปัญหา: "Failed to connect to Supabase"
**วิธีแก้:**
- ตรวจสอบ `.env` ว่าใส่ URL และ Key ถูกต้อง
- ตรวจสอบว่า Supabase Project ยังทำงานอยู่
- ลองรีสตาร์ท Dev Server: `Ctrl+C` แล้ว `npm run dev` ใหม่

### ปัญหา: "npm install" ล้มเหลว
**วิธีแก้:**
```bash
# ลบ node_modules และ lock file
rm -rf node_modules package-lock.json

# ติดตั้งใหม่
npm install
```

### ปัญหา: หน้าจอขาวเปล่า (Blank screen)
**วิธีแก้:**
1. เปิด Console ในเบราว์เซอร์ (`F12`)
2. ดู Error message
3. มักเกิดจาก:
   - ไม่ได้ใส่ `.env`
   - Database ไม่มี Tables
   - Supabase RLS Policies ยังไม่ถูกต้อง

---

## 📞 การสนับสนุน

หากมีปัญหาในการติดตั้ง:
1. ตรวจสอบ Error messages ใน Console
2. อ่าน Documentation:
   - Supabase: https://supabase.com/docs
   - Vite: https://vitejs.dev
   - React Router: https://reactrouter.com

---

## 📝 Checklist สำหรับการติดตั้ง

### สำหรับผู้สำรองข้อมูล (Admin เดิม)
- [ ] สำรอง Migration files จาก `supabase/migrations/`
- [ ] Push โค้ดขึ้น GitHub หรือสร้าง ZIP file
- [ ] สร้างไฟล์ `.env.example`

### สำหรับผู้ติดตั้งใหม่ (โรงเรียนใหม่)
- [ ] สร้าง Supabase Project
- [ ] รัน Migration files ทั้งหมด
- [ ] Clone/Download โค้ด
- [ ] ติดตั้ง `npm install`
- [ ] สร้างไฟล์ `.env` และใส่ค่า
- [ ] ทดสอบ `npm run dev`
- [ ] สร้าง Admin account แรก
- [ ] กรอกข้อมูลโรงเรียนใน Settings
- [ ] Deploy ขึ้น Vercel
- [ ] ตั้งค่า Domain (ถ้ามี)

---

## ⚡ Quick Start (สรุป)

```bash
# 1. Clone project
git clone https://github.com/YOUR_USERNAME/school-website-template.git
cd school-website-template

# 2. ติดตั้ง dependencies
npm install

# 3. สร้าง .env
cp .env.example .env
# (แก้ไขใส่ Supabase URL และ Key)

# 4. รัน development server
npm run dev

# 5. สร้าง Admin account ผ่าน Supabase Dashboard

# 6. เข้า http://localhost:5173/admin และตั้งค่าโรงเรียน
```

---

**สร้างโดย:** School Website System  
**เวอร์ชัน:** 1.0  
**อัปเดตล่าสุด:** ธันวาคม 2025
