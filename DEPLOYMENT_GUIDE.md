# คู่มือการติดตั้งระบบเว็บไซต์โรงเรียน (ฉบับสมบูรณ์)

คู่มือนี้ออกแบบมาเพื่อให้ครูหรือบุคลากรทางการศึกษาที่อาจไม่มีความรู้ด้านการเขียนโปรแกรม สามารถติดตั้งและนำเว็บไซต์นี้ไปใช้งานได้จริงกับโรงเรียนของท่าน

---

## 📋 สารบัญ
1.  [เตรียมความพร้อม](#1-เตรียมความพร้อม)
2.  [ติดตั้งฐานข้อมูล (Supabase)](#2-ติดตั้งฐานข้อมูล-supabase)
3.  [ติดตั้งเว็บไซต์](#3-ติดตั้งเว็บไซต์)
4.  [การปรับแต่งข้อมูลโรงเรียน](#4-การปรับแต่งข้อมูลโรงเรียน)
5.  [การนำขึ้นออนไลน์ (Deployment)](#5-การนำขึ้นออนไลน์-deployment)
6.  [การตั้งค่าโดเมนโรงเรียน](#6-การตั้งค่าโดเมนโรงเรียน-custom-domain)

---

> **⚠️ ข้อสำคัญสำหรับผู้เริ่มต้น:**
> เมื่อท่านดาวน์โหลดโปรเจกต์นี้มา **ท่านจะไม่เห็นโฟลเดอร์ชื่อ `node_modules`** ในตอนแรก ซึ่งเป็นเรื่องปกติสำหรับการเขียนโปรแกรม
>
> **ทำไม?** เพราะโฟลเดอร์นี้มีขนาดใหญ่มาก (หลายร้อย MB) และประกอบด้วยไฟล์นับหมื่นไฟล์ การส่งต่อไฟล์พวกนี้จะทำให้หนักเครื่องและล่าช้า
>
> **สิ่งที่ต้องทำ:** เพียงแค่ทำตามขั้นตอน "ติดตั้งเว็บไซต์" ในข้อ 3 โดยการพิมพ์คำสั่ง `npm install` ระบบจะทำการดาวน์โหลดไฟล์ที่จำเป็นทั้งหมดมาให้ท่านโดยอัตโนมัติ (เปรียบเสมือนการปรุงอาหารสดใหม่ แทนที่จะส่งอาหารแช่แข็งไปให้)

## 1. เตรียมความพร้อม

ก่อนเริ่มต้น ท่านต้องสมัครบัญชีเหล่านี้ (ฟรีทั้งหมด):

1.  **Github Account** (สำหรับเก็บโค้ด)
    - สมัครที่: [https://github.com](https://github.com)
2.  **Supabase Account** (สำหรับฐานข้อมูล)
    - สมัครที่: [https://supabase.com](https://supabase.com)
3.  **Vercel Account** (สำหรับนำเว็บขึ้นออนไลน์)
    - สมัครที่: [https://vercel.com](https://vercel.com) (แนะนำให้ Login ด้วย Github)
4.  **โปรแกรมที่ต้องลงในเครื่องคอมพิวเตอร์**:
    - [VS Code](https://code.visualstudio.com/) (สำหรับแก้ไขโค้ด)
    - [Node.js](https://nodejs.org/) (รุ่น LTS)
    - [Git](https://git-scm.com/)

---

## 2. ติดตั้งฐานข้อมูล (Supabase)

ระบบนี้ใช้ Supabase เป็นฐานข้อมูลในการเก็บข่าว รูปภาพ และข้อมูลนักเรียน

### 2.1 สร้างโปรเจกต์ใหม่
1.  เข้าสู่ระบบ [Supabase Dashboard](https://supabase.com/dashboard)
2.  กดปุ่ม **"New Project"**
3.  เลือก Organization (ถ้ามี)
4.  กรอกข้อมูล:
    - **Name**: ชื่อโรงเรียน (ภาษาอังกฤษ)
    - **Database Password**: ตั้งรหัสผ่าน (จดเก็บไว้ด้วย!)
    - **Region**: เลือก `Southeast Asia (Singapore)` เพื่อความเร็ว
5.  กด **"Create new project"** และรอประมาณ 1-2 นาที

### 2.2 ติดตั้งโครงสร้างฐานข้อมูล (Schema)
1.  เมื่อโปรเจกต์สร้างเสร็จแล้ว ให้มองหาเมนูทางซ้ายชื่อ **"SQL Editor"** (ไอคอนกระดาษที่มีคำว่า SQL)
2.  กด **"New query"** (พื้นที่ว่างๆ ทางขวา)
3.  กลับมาที่ไฟล์โค้ดของท่าน เปิดไฟล์ชื่อ `SUPABASE_SETUP.sql`
4.  **คัดลอก (Copy)** ข้อความทั้งหมดในไฟล์ `SUPABASE_SETUP.sql`
5.  นำไป **วาง (Paste)** ลงในช่อง SQL Editor ของ Supabase
6.  กดปุ่ม **"Run"** (ขวาล่าง)
7.  เมื่อขึ้นคำว่า "Success" แสดงว่าฐานข้อมูลพร้อมใช้งานแล้ว

### 2.3 ตั้งค่าพื้นที่เก็บรูปภาพ (Storage)
1.  ไปที่เมนู **"Storage"** (ไอคอนถังข้อมูล)
2.  กด **"New Bucket"**
3.  ตั้งชื่อว่า `school-images`
4.  เปิดสวิตช์ **"Public Bucket"** ให้เป็นสีเขียว (สำคัญมาก!)
5.  กด **"Create bucket"**

---

## 3. ติดตั้งเว็บไซต์

ท่านสามารถเลือกทำได้ 2 วิธี:

### วิธีที่ 1: ติดตั้งลงเครื่องเพื่อทดสอบ (Recommended)
1.  ดาวน์โหลดโค้ด (Clone Project) หรือแตกไฟล์ ZIP
2.  เปิดโฟลเดอร์โปรเจกต์ด้วยโปรแกรม **VS Code**
3.  เปิด Terminal ใน VS Code (`Ctrl + J` หรือ `View > Terminal`)
4.  พิมพ์คำสั่งเพื่อติดตั้งเครื่องมือเสริม (สำคัญมาก!):
    ```bash
    npm install
    ```
    > *รอสักครู่... ระบบจะสร้างโฟลเดอร์ `node_modules` ขึ้นมาให้เอง ขั้นตอนนี้อาจใช้เวลา 1-3 นาทีขึ้นอยู่กับความเร็วเน็ต*
5.  **ตั้งค่าการเชื่อมต่อฐานข้อมูล**:
    - สร้างไฟล์ใหม่ชื่อ `.env` (ดูตัวอย่างจาก `.env.example`)
    - ท่านต้องนำค่าจาก Supabase มาใส่ในไฟล์นี้:
        - ไปที่ Supabase > Settings (เฟือง) > API
        - ก๊อปปี้ **Project URL** มาใส่หลัง `VITE_SUPABASE_URL=`
        - ก๊อปปี้ **anon public key** มาใส่หลัง `VITE_SUPABASE_ANON_KEY=`
6.  **ทดลองรันโปรแกรม**:
    พิมพ์คำสั่ง:
    ```bash
    npm run dev
    ```
    จากนั้นกดลิงก์ `http://localhost:8080` เพื่อดูหน้าเว็บ

---

## 4. การปรับแต่งข้อมูลโรงเรียน

### 4.1 สร้าง Admin คนแรก
1.  ไปที่ Supabase > Authentication > Users
2.  กด **"Add User"**
3.  ใส่อีเมลและรหัสผ่านของผู้ดูแลระบบ (Admin)
4.  กด **"Create User"**

### 4.2 เข้าสู่ระบบจัดการ
1.  ไปที่หน้าเว็บของท่าน เข้าเมนู `/admin` หรือกดปุ่ม "เข้าระบบ"
2.  ล็อกอินด้วยอีเมลที่สร้างไว้
3.  ไปที่เมนู **"ตั้งค่าระบบ"**
4.  ท่านสามารถเปลี่ยน:
    - ชื่อโรงเรียน
    - ตราสัญลักษณ์ (Logo)
    - ข้อมูลติดต่อ
    - สีประจำโรงเรียน

---

## 5. การนำขึ้นออนไลน์ (Deployment)

เราจะใช้ **Vercel** ซึ่งฟรีและง่ายที่สุด

1.  **นำโค้ดขึ้น GitHub (ทำผ่าน VS Code ง่ายที่สุด)**:
    เราจะใช้วิธีที่ง่ายที่สุดคือการใช้ปุ่มในโปรแกรม VS Code ครับ **(ท่านไม่ต้องไปสร้าง Repository ในเว็บ GitHub รอไว้ก่อนนะครับ เดี๋ยวโปรแกรมจะสร้างให้เอง)**
    
    1.  เปิดโปรแกรม **VS Code** และเปิดโฟลเดอร์โปรเจกต์นี้
    2.  มองหาเมนูทางซ้ายมือที่เป็นรูป **กิ่งไม้ (Source Control)** หรือกดปุ่ม `Ctrl + Shift + G`
    3.  ท่านจะเห็นปุ่มสีฟ้าๆ เขียนว่า **"Publish to GitHub"** (หรือ "Initialize Repository" แล้วค่อยกด "Publish Branch")
    4.  กดปุ่มนั้น โปรแกรมอาจจะถามให้ท่านล็อกอิน GitHub (ถ้ายังไม่เคยล็อกอิน) ให้กด Allow/Authorize ตามขั้นตอน
    5.  จะมีช่องให้เลือกด้านบน ให้เลือก **"Publish to GitHub public repository"** (เพื่อให้ Vercel ดึงไปใช้ได้ง่าย)
    6.  **ระบบจะทำการสร้าง Repository ใหม่บน GitHub ให้ท่านอัตโนมัติ** และอัปโหลดไฟล์ขึ้นไปให้ทันที
    7.  รอสักครู่... เมื่อเสร็จแล้วจะมีหน้าต่างแจ้งเตือนที่มุมขวาล่าง ให้กด **"Open on GitHub"** เพื่อตรวจสอบว่าไฟล์ขึ้นไปอยู่บนเว็บแล้ว

2.  **เชื่อมต่อ Vercel**:
    - ไปที่ [Vercel Dashboard](https://vercel.com/dashboard)
    - กด **"Add New..."** > **"Project"**
    - เลือก Import จาก GitHub และเลือก Repository ที่ท่านสร้างไว้

3.  **ตั้งค่า Environment Variables (สำคัญ)**:
    - ในหน้าตั้งค่าโปรเจกต์ของ Vercel ให้หาหัวข้อ "Environment Variables"
    - ใส่ค่าเหมือนที่ทำในไฟล์ `.env`:
        - `VITE_SUPABASE_URL` : (ค่า URL)
        - `VITE_SUPABASE_ANON_KEY` : (ค่า Key)
    
4.  กด **"Deploy"**
    - รอสักครู่ ท่านจะได้รับลิงก์เว็บไซต์ของโรงเรียนที่ใช้งานได้จริง (เช่น `school-name.vercel.app`)
    - ท่านสามารถนำโดเมน `.ac.th` ของโรงเรียนมาผูกได้ตามขั้นตอนด้านล่าง

---

## 6. การตั้งค่าโดเมนโรงเรียน (Custom Domain)

หากโรงเรียนของท่านมีโดเมนจดทะเบียนไว้แล้ว (เช่น `www.school-name.ac.th`) ท่านสามารถนำมาเชื่อมต่อได้ดังนี้:

### 6.1 ตั้งค่าใน Vercel
1.  ไปที่หน้าโปรเจกต์ของท่านใน **Vercel**
2.  ไปที่เมนู **Settings** (ด้านบน) -> **Domains** (ด้านซ้าย)
3.  ในช่องใส่โดเมน ให้พิมพ์ชื่อโดเมนของท่านลงไป (แนะนำให้ใส่ `www.` นำหน้า เช่น `www.โรงเรียนของฉัน.ac.th`)
4.  กดปุ่ม **Add**
5.  ระบบจะขึ้นแจ้งเตือนสีแดงหรือเหลือง พร้อมแสดงค่าที่ท่านต้องนำไปตั้งค่า (Invalid Configuration) ไม่ต้องตกใจ

### 6.2 ตั้งค่า DNS (ที่ผู้ให้บริการโดเมนของท่าน)
ท่านต้องล็อกอินเข้าไปในเว็บไซต์ที่ท่านจดโดเมนไว้ (เช่น GoDaddy, Namecheap, HostAtom หรือผู้ให้บริการ .th) และไปที่หน้าตั้งค่า DNS

**กรณีที่ 1: ใช้ Subdomain (เช่น www)**
เป็นการตั้งค่าให้ `www.school.ac.th`
- **Type**: `CNAME`
- **Name**: `www`
- **Value**: `cname.vercel-dns.com`

**กรณีที่ 2: ใช้ Root Domain (เช่น school.ac.th - ไม่มี www)**
- **Type**: `A`
- **Name**: `@` (หรือว่างไว้)
- **Value**: `76.76.21.21`

> *เมื่อตั้งค่าเสร็จแล้ว ให้รอประมาณ 1-24 ชั่วโมง (ปกติจะไม่กี่นาที) กลับมาดูที่ Vercel ถ้าขึ้นสีเขียวและมีเครื่องหมายติ๊กถูก แสดงว่าใช้งานได้แล้ว!*

---
**ขอให้มีความสุขกับการใช้งานระบบเว็บไซต์โรงเรียนครับ!**
